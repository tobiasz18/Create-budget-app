import {
  BUDGET_GET,
  BUDGET_GET_FAILURE,
  BUDGET_GET_REQUEST,
  BUDGET_GET_SUCCESS,
} from 'data/constants';

const fetchBudget = () => (dispatch) => {
  dispatch({
    type: BUDGET_GET_REQUEST,

  })

  
 
}

const fetchBudgetCategories = () => {
  
}